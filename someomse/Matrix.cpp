#include "Matrix.h"

/*template <typename T>
istream& operator>> (istream& in, Matrix<T>& fr) {
	in >> fr.values >> fr.colms >> fr.rows >> fr.name;

	return in;
}
template <typename T>
ostream& operator<< (ostream& oss, const Matrix<T>& fr) {
	return os << fr.values;
}*/